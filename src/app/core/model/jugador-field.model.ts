export interface JugadorField {
    name: string,
    type: string,
    viewName: string,
    required: boolean,
    group: [],
    esSkill: boolean
    esNombre: boolean,
    minVal: number,
    maxVal: number,
    minLen: number,
    maxLen: number
}