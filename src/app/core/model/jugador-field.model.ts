export interface JugadorField {
    name: string,
    type: string,
    viewName: string,
    esSkill: boolean
    esNombre: boolean,
    minVal: number,
    maxVal: number,
    minLen: number,
    maxLen: number
}